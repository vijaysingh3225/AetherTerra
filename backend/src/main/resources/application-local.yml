spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/aetherterra
    username: aetherterra
    password: aetherterra
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
  mail:
    host: localhost
    port: 1025
    properties:
      mail.smtp.auth: false
      mail.smtp.starttls.enable: false

aetherterra:
  app:
    base-url: http://localhost:5173

logging:
  level:
    com.aetherterra: DEBUG
    org.flywaydb: INFO
